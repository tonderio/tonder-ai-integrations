# Release 0.1.4

## Summary

Patch release for robust Tonder Web SDK documentation lookup.

## Fixed

- `get_sdk_api_reference` now falls back to searching section content when a topic is not a section title or explicit alias.
- Verified lookup for `customization`, `styles`, `TonderCustomization`, `TonderConfig`, `CardPlaceholders`, `PaymentMethodBank`, `secure_token`, and `SECURE_FIELDS_LOAD_ERROR`.

## Verification

- `npm test`
- `npm run build`
- JSON-RPC MCP smoke test across representative topic aliases and content-only symbols
- `claude plugin validate ./plugins/claude-code/tonder-web-sdk`
- `claude plugin validate .`
- Codex plugin validation
