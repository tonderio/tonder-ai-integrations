# Release 0.1.6

## Summary

Patch release correcting secure-field style guidance.

## Fixed

- Clarifies that `customization.card_fields.styles` may style SDK-rendered secure input internals, including input-level sizing when supported by the renderer.
- Clarifies that merchant CSS is still needed to reserve layout space for the secure-field mount container.

## Verification

- `npm test`
- `npm run build`
- `claude plugin validate ./plugins/claude-code/tonder-web-sdk`
- `claude plugin validate .`
- Codex plugin validation
